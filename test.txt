include TLib.IODefs
include TLib.MathDefs

var b = 0.0;
var a = 0.0;
var c = 0.0;

input(a, b);

c = cos(a) + sin(b);

print("cos + sin of a and b is: %lf", c);

var d = 0.0;

input(d);
print("Factorial of d is: %lf", factorial(d));